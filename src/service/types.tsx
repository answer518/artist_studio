export type Nft = {
  tokenId: string;
  tokenUri: string;
  name: string;
  description: string;
  imageUri: string;
};

export type NftMeta = {
  name: string;
  description: string;
  imageUri: string;
};
